'use client';

import React from 'react';

interface ParticipantFilesClientProps {
}

const ParticipantFilesClient: React.FC<ParticipantFilesClientProps> = (props) => {
    // download file
    // load more
    // delete file
    return (
        <p>ParticipantFilesClient</p>
    );
};

export default ParticipantFilesClient;
